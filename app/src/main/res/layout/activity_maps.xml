<fragment xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:map="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/map"
    android:name="com.google.android.gms.maps.MapFragment"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="motohud.fydp.com.motohud.maps.MapsActivity"
    >

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        <Button
            android:id="@+id/maps_scan_button"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:padding="8dp"
            android:layout_marginTop="10dp"
            android:layout_marginBottom="5dp"
            android:background="@android:color/white"
            android:text="@string/maps_scan_button_text">
        </Button>

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_gravity="center"
            android:gravity="center"
            android:theme="@style/Base.Widget.AppCompat.ButtonBar">
            <Button
                android:id="@+id/maps_transmit_navigation_button"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:padding="8dp"
                android:layout_marginEnd="10dp"
                android:text="@string/maps_transmit_button_disabled_navigation_text"
                android:background="@android:color/darker_gray"
                android:clickable="false"
                android:theme="@style/Base.Widget.AppCompat.ButtonBar">
            </Button>

            <Button
                android:id="@+id/maps_transmit_state_button"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:padding="8dp"
                android:text="@string/maps_transmit_button_disabled_state_text"
                android:background="@android:color/darker_gray"
                android:clickable="false"
                android:theme="@style/Base.Widget.AppCompat.ButtonBar">
            </Button>
        </LinearLayout>
    </LinearLayout>
    <android.support.design.widget.FloatingActionButton
        android:id="@+id/fab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="end|bottom"
        android:src="@drawable/ic_navigation_black"
        android:layout_margin="16dp" />
</fragment>
